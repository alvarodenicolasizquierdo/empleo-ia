[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["find node_modules -maxdepth 1 -not -name '.cache' -not -name 'node_modules' -exec rm -rf {} + && rm -f package-lock.json && npm install && npm run build"]
